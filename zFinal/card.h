#pragma once
#include"player.h"
using namespace std;

class Card {
    private:
    Player owner; // Owner of the card.
    string name; // Name of the card.
    // THIS IS NOT DONE.

    public:
    Card(string name); 
};